name: resizable_splitter_example
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  resizable_splitter:
    path: ../
  webview_flutter: ^4.13.0
  macos_ui: ^2.2.2
  # Provides the CupertinoIcons font used by the native macOS demo's toolbar
  # and sidebar icons; without it those glyphs do not render.
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true



